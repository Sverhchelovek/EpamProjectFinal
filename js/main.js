function getRandomInt(min, max){
 	const number = Math.floor(Math.random() * (max - min + 1)) + min;
 	return number;
 	console.log(number);
}

let randomNumber = getRandomInt(1, 100);


function myDesicion(){
	let myNumber = prompt('Choose your number');
	if (myNumber === randomNumber) {
		console.log('Congratulations! U won');
	} else {
		let restNumber = myNumber - 
	}
}

myDesicion();

a5	`	aw23w2`